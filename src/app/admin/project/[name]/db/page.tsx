import { DbPage } from "@/ui/project/components/DbPage";
import { Stack, Title } from "@mantine/core";

export default function Page({ params }: { params: { name: string } }) {
    return <DbPage />
}